<Window
    x:Class="ExifDateFixer.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="EXIF Date Fixer">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with title and version -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              Padding="24,16">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEB9F;" FontSize="32" />
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="EXIF Date Fixer" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock x:Name="VersionTextBlock" Text="Version 1.0.0" 
                               Style="{StaticResource CaptionTextBlockStyle}" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Settings Panel -->
        <StackPanel Grid.Row="1" Padding="24" Spacing="16">
            <TextBlock Text="Settings" Style="{StaticResource SubtitleTextBlockStyle}" />
            
            <!-- Path Selection -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathTextBox" 
                         Grid.Column="0"
                         Header="Folder Path"
                         PlaceholderText="Select a folder to scan for images..."
                         IsReadOnly="True"
                         Margin="0,0,8,0"/>
                <Button x:Name="BrowseButton" 
                        Grid.Column="1"
                        Content="Browse..." 
                        Click="BrowseButton_Click"
                        VerticalAlignment="Bottom"
                        Style="{StaticResource AccentButtonStyle}"/>
            </Grid>

            <!-- Options -->
            <StackPanel Spacing="12">
                <CheckBox x:Name="RecursiveCheckBox" 
                          Content="Scan subdirectories recursively"
                          IsChecked="True"/>
                <CheckBox x:Name="DryRunCheckBox" 
                          Content="Dry run (preview changes without modifying files)"
                          IsChecked="False"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="StartButton" 
                        Content="Start Processing" 
                        Click="StartButton_Click"
                        Style="{StaticResource AccentButtonStyle}"/>
                <Button x:Name="ClearLogButton" 
                        Content="Clear Log" 
                        Click="ClearLogButton_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Log Viewer -->
        <Grid Grid.Row="2" Padding="24,0,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Processing Log" 
                       Style="{StaticResource SubtitleTextBlockStyle}" 
                       Margin="0,0,0,8"/>
            
            <Border Grid.Row="1" 
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <ScrollViewer x:Name="LogScrollViewer" 
                              VerticalScrollBarVisibility="Auto"
                              Padding="12">
                    <TextBlock x:Name="LogTextBlock" 
                               TextWrapping="Wrap"
                               FontFamily="Consolas"
                               FontSize="12"
                               IsTextSelectionEnabled="True"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Grid Grid.Row="3" 
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              Padding="24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="StatusTextBlock" 
                       Text="Ready" 
                       VerticalAlignment="Center"
                       Style="{StaticResource BodyTextBlockStyle}"/>
            
            <ProgressBar x:Name="ProgressBar" 
                         Grid.Column="1"
                         Width="200"
                         IsIndeterminate="False"
                         Visibility="Collapsed"
                         VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
